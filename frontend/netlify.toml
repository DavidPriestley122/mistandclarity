[build]
  command = "npm run build"
  publish = "dist"

# SPA routing - redirect to index.html only if no static file exists
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
